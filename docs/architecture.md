# Architecture Overview

This document has moved. The maintained, code-accurate architecture description lives in [ARCHITECTURE.md](ARCHITECTURE.md).

The previous version of this file described a few components that were never actually implemented (a standalone `AuditLogService`, an `AES256EncryptionService` strategy class, a `/metrics` endpoint). Rather than keep two documents drifting apart, everything was consolidated into [ARCHITECTURE.md](ARCHITECTURE.md) and grounded in the real code.
