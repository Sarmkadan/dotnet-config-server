# Application

Represents a logical application entity within the `dotnet-config-server` system. This type encapsulates metadata, security credentials, and configuration tracking for a single application, enabling centralized management of application-specific settings, versioning, and access control. It serves as the primary container for configurations, audit logs, and lifecycle events associated with an application instance.

## API

### Properties

#### `Id`
