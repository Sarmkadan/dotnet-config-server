#nullable enable
sealed using Microsoft.AspNetCore.Mvc; using System.Threading.Tasks; namespace dotnet_config_server.Controllers { [ApiController] [Route("[controller]")] public class HealthCheckController : ControllerBase { [HttpGet] public async Task<IActionResult> Get() { return Ok(new { status = "healthy" }); } [HttpGet("ready")] public async Task<IActionResult> GetReady() { // Add dependency checks here return Ok(new { status = "ready" }); } } }
